{
  "entities": {
    "UserProfile": {
      "title": "User Profile",
      "description": "Represents a user's public and private profile data.",
      "type": "object",
      "properties": {
        "email": { "type": "string", "format": "email" },
        "displayName": { "type": "string" },
        "photoURL": { "type": "string", "format": "uri" }
      },
      "required": ["email"]
    },
    "Project": {
      "title": "Project",
      "description": "Represents a generated website project, including all its files and metadata.",
      "type": "object",
      "properties": {
        "userId": { "type": "string", "description": "The UID of the user who owns the project." },
        "name": { "type": "string", "description": "The name of the project." },
        "prompt": { "type": "string", "description": "The prompt used to generate the project." },
        "previewContent": { "type": "string", "description": "The HTML content for the live preview." },
        "files": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "path": { "type": "string" },
              "content": { "type": "string" }
            },
            "required": ["path", "content"]
          }
        },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["userId", "name", "prompt", "createdAt"]
    }
  },
  "auth": {
    "providers": ["password", "google.com"]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": { "$ref": "#/entities/UserProfile" },
      "description": "Stores basic user profile information. Each user can only access their own document."
    },
    "/users/{userId}/projects/{projectId}": {
        "schema": {"$ref": "#/entities/Project"},
        "description": "Stores the projects created by a specific user. Each user can only access their own projects."
    }
  }
}
